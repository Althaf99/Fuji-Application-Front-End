import { makeStyles } from "@mui/styles";

export const styles = makeStyles((theme) => ({
  container: {
    flexDirection: "row",
  },
  section: {
    paddingTop: "10px",
  },
  textField: {
    width: "100%",
  },
}));
